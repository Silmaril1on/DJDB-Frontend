import FestivalForm from "@/app/createforms/FestivalForm";

export const metadata = {
  title: "DJDB - Create Festival",
};

const CreateFestivalPage = () => {
  return <FestivalForm />;
};

export default CreateFestivalPage;
